# Historical C++ golden fixtures carry recorded SHA-256 digests in
# MANIFEST.sha256. Git's Windows autocrlf would rewrite LF into CRLF on
# checkout and change those digests, so the fixtures must stay byte-for-byte
# identical on every platform.
fixtures/cpp-golden/** -text

# Reference audio/screenshot assets are byte-exact by contract.
fixtures/** -text
